extends layout.pug
block content
        h2 Welcome to the Hello Page
        form(action='/hello',method='post')
            label What is your username?
                input(type="text", name='username')
            button(type='submit') Submit

